apiVersion: v1
kind: Service
metadata:
  name: computesdk-sidekick
  namespace: computesdk-system
  labels:
    app: computesdk-sidekick
spec:
  selector:
    app: computesdk-sidekick
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP